# RESUME — Leaders 5-feature build (checkpoint 2026-07-01)

Start-here doc for continuing the build in a fresh session **with the Supabase MCP connected**.

## Where we are
- **Branch:** `feat/leaders-feedback-5-features` (off `main`). All work is committed here (except possibly Phase 5 — verify below).
- **Plan:** [plans/2026-07-01-leaders-feedback-5-features.md](plans/2026-07-01-leaders-feedback-5-features.md) — 5 phases, full code.
- **Canva setup runbook:** [../canva-connect-setup-runbook.md](../canva-connect-setup-runbook.md).
- **Type-check:** `npx tsc --noEmit` = **0 errors** across phases 1–4 (re-run after confirming Phase 5).

### Phase status
| Phase | What | Committed? |
|---|---|---|
| 1 | recipients module + email Roei on kickoff completion | ✅ |
| 2 | manual meeting-date fields + cron "day before" reminders | ✅ |
| 3 | Canva Connect (OAuth PKCE + url-import) — live-verified `/api/canva/oauth/start` | ✅ |
| 4 | deck approve endpoint + auto influencer-brief document | ✅ |
| 5 | batch influencer contracts from signed quote | **verify `git log`** |

**Verify Phase 5 first:** `git log --oneline main..HEAD`. If you don't see Phase 5 commits and `git status` shows untracked `src/lib/influencer-contract/`, `src/lib/templates/influencer-contract-template.ts`, `src/app/api/quotes/[id]/influencer-contracts/`, `src/app/quotes/[id]/influencer-contracts/` — finish/commit Phase 5 per the plan (lines 2458+), then `npx tsc --noEmit`.

## Step 1 — Apply the 4 pending migrations (Supabase MCP now connected)
Project ref **`fhgggqnaplshwbrzgima`**. All idempotent + additive. Apply via `apply_migration` (or SQL editor):
1. `supabase/migrations/20260701_meeting_dates_and_reminders.sql`
2. `supabase/migrations/20260702_canva_integration.sql`  ← creates `canva_tokens` (needed before Canva connect)
3. `supabase/migrations/20260420_deck_approval_influencer_brief.sql`
4. `supabase/migrations/20260428_influencer_contracts.sql`

Then sanity-check the new columns exist (e.g. `inner_meeting_forms.canva_edit_url`, `documents.approved_at`, `signature_requests.parent_signature_request_id`) and that `to_regclass('public.canva_tokens')` is non-null.

> Note: if `documents.type` has a CHECK constraint, broaden it to allow `'influencer_brief'` (Phase 4). The seed migration didn't add one, but confirm.

## Step 2 — Finish the Canva connection
- Canva env vars are in `.env.local`; add the same 4 to **Vercel** (all envs): `CANVA_CLIENT_ID`, `CANVA_CLIENT_SECRET`, `CANVA_REDIRECT_URI`, `CANVA_SCOPES`. **Never** put a Canva refresh token in ENV (it rotates → lives in `canva_tokens`).
- Deploy the branch (or run locally with a `localhost` redirect also registered in the Canva portal).
- Visit `https://leaders-platform.vercel.app/api/canva/oauth/start` while signed into the **Leaders service Canva account** → **Allow** → lands on `/dashboard?canva=connected`, `canva_tokens` row populated.
- Smoke-test import: `POST /api/canva/import { "documentId": "<a real deck id>" }` → expect `edit_url` in the JSON.

## Step 3 — Remaining polish (optional / follow-up)
- Full `npm run build`.
- Deck-UI **approve** button: Phase 4's `GET /api/documents/[id]` now returns `influencerBrief`, but no approve button was added — wire one that POSTs `/api/documents/[id]/approve`.
- Wire `inner_meeting_forms.linked_deck_document_id` (manual deck↔kickoff link — design assumption #1) so the Canva link and day-before reminder attach to the right kickoff.

## Test safety (do not skip)
- `NOTIFICATIONS_TEST_MODE=true` (+ optional `NOTIFICATIONS_TEST_RECIPIENT`) routes ALL notification sends to a single safe address (default `cto@`).
- Never email Roei/Sharon/real management during tests; **never** `eran@ldrsgroup.com`.
